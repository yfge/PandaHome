## Summary
- Aligned backend/frontend environment templates and documentation with the new configuration defaults.

## Changes
- Added `api/.env.example` and `web/.env.local.example` so developers can bootstrap environment files quickly.
- Updated `.gitignore` (root and `web/.gitignore`) to track the new example files.
- Refreshed `README.md` and `README.zh.md` with unified install commands, environment setup steps, and a shared variables table.
- Checked off Feature 01 Step 6 in `tasks.md`.

## Outcome
- Contributors now have source-controlled templates and accurate docs describing required variables and toolchain usage.

## Verification
- Not run – documentation/configuration update only.

## Next steps
- Begin Feature 02 Step 1 (backend auth settings/dependencies) when ready.
