## Summary
- Hardened the frontend around authenticated flows by handling 401 responses and hiding protected navigation when logged out.

## Changes
- Extended the shared API client with global bearer-token injection and an unauthorized handler hook; AuthProvider now auto-clears tokens and redirects to `/login` on 401s (`web/src/lib/api-client.ts`, `web/src/components/providers/auth-provider.tsx`).
- Updated the main navigation, protected wrapper, and login page to react to auth state, showing only accessible links and redirecting authenticated users appropriately (`web/src/components/layout/main-nav.tsx`, `web/src/components/auth/protected.tsx`, `web/src/app/login/page.tsx`).
- Guarded status/domains/UPnP pages via the `Protected` component and localized supporting strings; marked Feature 02 Step 6 complete (`web/src/app/*`, `web/src/i18n/locales/*.json`, `tasks.md`).

## Outcome
- Unauthorized API responses now trigger a clean logout and redirect, and unauthenticated users no longer see or reach management pages without logging in.

## Verification
- `npm run lint --prefix web`
- `pytest`

## Next steps
- Proceed to Feature 02 Step 7 to document the end-to-end auth lifecycle.
