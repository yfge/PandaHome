## Summary
- Added the missing backend runtime dependencies to `api/pyproject.toml` and refreshed the audit notes.

## Changes
- Updated `api/pyproject.toml` dependencies to include `aiohttp`, `async-upnp-client`, `bcrypt`, `netifaces`, `passlib[bcrypt]`, `python-jose[cryptography]`, and `python-multipart`.
- Amended `docs/backend-dependency-audit.md` to capture the `netifaces` requirement noted during implementation.
- Marked Feature 01 Step 2 complete in `tasks.md`.

## Outcome
- Backend packaging now declares all runtime dependencies required by the UPnP and auth modules, ready for installation with `uv`.

## Verification
- `python3 -m pip install uv`
- `~/Library/Python/3.9/bin/uv pip install --system -e api`

## Next steps
- Move on to Feature 01 Step 3: normalize configuration defaults and `.env` handling.
