## Summary

- Completed the remaining authentication documentation step and refactored the UPnP backend service to be dependency-injected with clearer error handling.

## Changes

- `api/src/app/services/upnp.py`: Removed singleton pattern, added `UPNPErrorCode` + `UPNPServiceError`, made mapping listing async with cache refresh, added input validation, and refreshed caches after add/delete.
- `api/src/app/routers/upnp.py`: Injected `UPNPService` via `Depends`, awaited async service calls, and mapped typed service errors to HTTP status codes.
- `api/src/app/main.py`: Mounted a single `UPNPService` instance on `app.state` and wired explicit startup/shutdown calls.
- `web/src/lib/api-client.ts`: Improved error message extraction to support FastAPI `{"detail": ...}` payloads (and validation error lists).
- `web/src/config/api.ts`: Aligned base URL env var to `NEXT_PUBLIC_API_BASE_URL` (with fallback to legacy `NEXT_PUBLIC_API_BASE`).
- `README.md`, `README.zh.md`: Linked `docs/auth-lifecycle.md` from the auth overview; corrected the UPnP delete endpoint placeholder.
- `tasks.md`: Marked Feature 02 Step 7 and Feature 03 Steps 1–2 as complete.
- `api/src/app/routers/domains.py`, `api/tests/conftest.py`: Fixed Ruff lint issues (unused import + import order) to keep checks green.

## Outcome

- UPnP API calls now consistently await async operations, return fresher mapping data, and return clearer HTTP errors when the service is unavailable or inputs are invalid.
- Auth docs are discoverable from both READMEs and include env + bootstrap guidance via `docs/auth-lifecycle.md`.

## Verification

- `pytest -q`
- `ruff check api`
- `npm run lint --prefix web`

## Next steps

- Implement Feature 03 Step 3: add pytest coverage for `/api/upnp/*` endpoints with a mocked UPnP service/device layer.
- Implement Feature 03 Step 4–6: mapping CRUD UI + UX states, and add `docs/upnp.md` troubleshooting guide.
