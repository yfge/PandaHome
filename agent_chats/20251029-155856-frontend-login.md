## Summary
- Implemented the frontend authentication experience with login form, token storage, and route protection.

## Changes
- Added `AuthProvider` with context-based token management, automatic `/api/users/me` hydration, and a reusable `Protected` gate (`web/src/components/providers/auth-provider.tsx`, `web/src/components/auth/protected.tsx`).
- Enhanced the shared API client with bearer-token injection utilities and created a reusable input component for forms (`web/src/lib/api-client.ts`, `web/src/components/ui/input.tsx`).
- Built the `/login` page, updated navigation to reflect auth state, and guarded status/domains/UPnP pages (`web/src/app/login/page.tsx`, `web/src/components/layout/user-nav.tsx`, `web/src/app/*`).
- Localized new auth strings in EN/ZH locale files and refreshed the home page call-to-action (`web/src/i18n/locales/en.json`, `web/src/i18n/locales/zh.json`, `web/src/app/page.tsx`).
- Marked Feature 02 Step 5 complete in `tasks.md`.

## Outcome
- Users can authenticate via a dedicated login page, persist tokens between sessions, access protected dashboards, and log out cleanly.

## Verification
- `npm run lint --prefix web`
- `pytest`

## Next steps
- Tackle Feature 02 Step 6 to propagate auth headers to the API client on the frontend and guard navigation globally.
