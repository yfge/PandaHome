## Summary
- Added backend tests exercising token issuance and admin/user RBAC flows with FastAPI’s TestClient.

## Changes
- Created pytest fixtures to spin up an isolated SQLite database, disable UPnP startup, and seed admin/normal users (`api/tests/conftest.py`).
- Wrote `api/tests/test_auth_users.py` covering success/error paths for `/api/auth/token` and `/api/users` endpoints.
- Tweaked auth dependencies to return tokens correctly for DI, made user schemas tolerate null `updated_at`, and marked Feature 02 Step 4 as complete.

## Outcome
- Test suite now validates happy-path token grants, rejects bad credentials, and ensures admin-only protections behave as expected.

## Verification
- `python3 -m compileall api/src api/tests`
- `pytest`

## Next steps
- Continue with Feature 02 Step 5 (frontend login experience).
